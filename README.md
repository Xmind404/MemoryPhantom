# MemoryPhantom
C++ library for reading and writing process memory on Windows. Supports pointers, module bases, and multiple data types. Inspired by Swed64 (MIT Licensed), fully reimplemented in C++ for performance and flexibility.
